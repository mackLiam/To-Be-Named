version: 2
updates:
  # GitHub Actions workflow deps
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]

  # NOTE: add these once the packages exist (Phase 1), otherwise Dependabot
  # errors on missing manifests:
  #
  # - package-ecosystem: npm
  #   directory: /
  #   schedule: { interval: weekly }
  #   groups:
  #     npm-minor-patch:
  #       update-types: [minor, patch]
  #
  # - package-ecosystem: pip
  #   directory: /services/pipeline
  #   schedule: { interval: weekly }
